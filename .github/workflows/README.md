# GitHub Actions Disabled

GitHub Actions are controllably deactivated for this repository for now.

Reason: preserve limited GitHub Actions minutes for essencefromexistence during the current month.

Do not restore workflow YAML files into this directory until essencefromexistence explicitly asks to re-enable GitHub Actions.

Original workflow files were moved to `../workflows-disabled/` with a `.disabled` suffix.